DB_HOST=localhost
DB_PORT=5431
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=bulletin_board

# Optional: If set, this takes precedence over the above.
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/bulletin_board
